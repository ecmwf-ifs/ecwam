      SUBROUTINE OUTWNORM

! ----------------------------------------------------------------------

!**** *WAMNORM* - PRINTS AVERAGE, MINIMUM AND MAXIMUM VALUES OF
!                 INTEGRATED PARAMETER FIELDS 

!     J. BIDLOT       ECMWF   JULY  1998 
!     D. PETTENUZZO   ADDED 9 NEW PARAMETERS FOR 3 WAVE TRAINS

!*    PURPOSE.
!     --------


!**   INTERFACE.
!     ----------

!     METHOD.
!     -------

!     EXTERNALS.
!     ----------

!       NONE.

!     REFERENCE.
!     ----------

!       NONE.

! ----------------------------------------------------------------------

      USE YOWCOUT  , ONLY : FFLAG    ,PFLAG    ,GFLAG    ,NFLAG    ,
     &            NFLAGALL ,NTRAIN   ,IPFGTBL
      USE YOMHOOK   ,ONLY : LHOOK,   DR_HOOK
      USE YOWINTP  , ONLY : WHGTTG   ,WDIRTG   ,WPKFTG   ,WMNFTG   ,
     &            USTARG   ,UDIRG    ,TAUWG    ,CDG      ,SMEANG   ,
     &            U10G     ,MWP1G    ,MWP2G    ,WSPRDG   ,C4G      ,
     &            BFG      ,QPG      ,DEPTHG   ,HMAXG    ,TMAXG    ,
     &            USTOKESG ,VSTOKESG ,UCURG    ,VCURG    ,PHIEPSG  ,
     &            PHIAWG   ,TAUOCG   ,STRNMSG  ,RHOAG    ,WSTARG   ,
     &            CICG     ,CIHG     ,C3G      ,E10G     ,
     &            NSSTG    ,NICCG    ,NICTG    ,NUCUG    ,NVCUG    ,
     &            WEFLXMG  ,WEFLXDG  ,E1012G   , 
     &            E1214G   ,E1417G   ,E1721G   ,E2125G   ,E2530G   ,
     &            WX1G     ,WX2G     ,WX3G     ,WX4G     ,WX5G
      USE YOWINTS  , ONLY : WHGTSG   ,WDIRSG   ,WMNFSG   ,WHGTWG   ,
     &            WDIRWG   ,WMNFWG   ,MWP1SG   ,MWP2SG   ,WSPRDSG  ,
     &            MWP1WG   ,MWP2WG   ,WSPRDWG
      USE YOWINTT  , ONLY : WHGTTRG  ,WDIRTRG  ,MWPMTRG
      USE YOWMESPAS, ONLY : LMESSPASS
      USE YOWMPP   , ONLY : IRANK    ,KTAG
      USE YOWPARAM , ONLY : NGX      ,NGY
      USE YOWPCONS , ONLY : EPSMIN   ,ZMISS
      USE YOWSTAT  , ONLY : CDTPRO
      USE YOWTEST  , ONLY : IU06
! ----------------------------------------------------------------------
      IMPLICIT NONE

      INTEGER :: ITG, ITT, NAVG
      REAL :: ZHOOK_HANDLE
      REAL :: FAVG,FMIN,FMAX

! ----------------------------------------------------------------------
#ifdef ECMWF
      IF (LHOOK) CALL DR_HOOK('OUTWNORM',0,ZHOOK_HANDLE)
#endif

      IF(NFLAGALL) THEN

        IF(IRANK.EQ.1) THEN
          WRITE(IU06,*) ' ' 
          WRITE(IU06,*) '   WAMNORM ON ',CDTPRO
          WRITE(IU06,*) '          AVERAGE            MINIMUM  ',
     &     '           MAXIMUM        SEA POINTS' 
        ELSE
          WRITE(IU06,*) ' ' 
          WRITE(IU06,*) '   WAMNORM ON ',CDTPRO,' SEE PE 1 OUTPUT'
        ENDIF

111     FORMAT(4x,'HEX: ',3(Z16.16,2x))

        IF(NFLAG(1).AND.(GFLAG(1).OR.FFLAG(1).OR.PFLAG(1))) THEN
          CALL WAMNORM(WHGTTG,NGX,NGY,ZMISS,EPSMIN,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(1),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR TOTAL WAVE HEIGHT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(2).AND.(GFLAG(2).OR.FFLAG(2).OR.PFLAG(2))) THEN
          CALL WAMNORM(WDIRTG,NGX,NGY,ZMISS,EPSMIN,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(2),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WAVE MEAN DIRECTION'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(3).AND.(GFLAG(3).OR.FFLAG(3).OR.PFLAG(3))) THEN
          CALL WAMNORM(WMNFTG,NGX,NGY,ZMISS,EPSMIN,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(3),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WAVE MEAN PERIOD'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(4).AND.(GFLAG(4).OR.FFLAG(4).OR.PFLAG(4))) THEN
          CALL WAMNORM(USTARG,NGX,NGY,ZMISS,EPSMIN,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(4),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR FRICTION VELOCITY'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(5).AND.(GFLAG(5).OR.FFLAG(5).OR.PFLAG(5))) THEN
          CALL WAMNORM(UDIRG,NGX,NGY,ZMISS,EPSMIN,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(5),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WIND DIRECTION'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(6).AND.(GFLAG(6).OR.FFLAG(6).OR.PFLAG(6))) THEN
          CALL WAMNORM(WPKFTG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(6),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WAVE PEAK FREQUENCY: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(7).AND.(GFLAG(7).OR.FFLAG(7).OR.PFLAG(7))) THEN
          CALL WAMNORM(CDG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(7),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR DRAG COEFFICIENT: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(8).AND.(GFLAG(8).OR.FFLAG(8).OR.PFLAG(8))) THEN
          CALL WAMNORM(TAUWG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(8),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WAVE INDUCED STRESS: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(9).AND.(GFLAG(9).OR.FFLAG(9).OR.PFLAG(9))) THEN
          CALL WAMNORM(SMEANG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(9),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR MEAN SQUARE SLOPE: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(10).AND.(GFLAG(10).OR.FFLAG(10).OR.PFLAG(10))) THEN
          CALL WAMNORM(U10G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(10),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WAVE WIND SPEED: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(11).AND.(GFLAG(11).OR.FFLAG(11).OR.PFLAG(11))) THEN
          CALL WAMNORM(WHGTWG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(11),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR WIND SEA WAVE HEIGHTS: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(12).AND.(GFLAG(12).OR.FFLAG(12).OR.PFLAG(12))) THEN
          CALL WAMNORM(WHGTSG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(12),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM FOR SWELL WAVE HEIGHTS: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(13).AND.(GFLAG(13).OR.FFLAG(13).OR.PFLAG(13))) THEN
          CALL WAMNORM(WDIRWG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(13),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM WIND SEA WAVE DIRECTION: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(14).AND.(GFLAG(14).OR.FFLAG(14).OR.PFLAG(14))) THEN
          CALL WAMNORM(WDIRSG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(14),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM SWELL WAVE DIRECTION: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(15).AND.(GFLAG(15).OR.FFLAG(15).OR.PFLAG(15))) THEN
          CALL WAMNORM(WMNFWG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(15),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM WIND SEA MEAN PERIOD: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(16).AND.(GFLAG(16).OR.FFLAG(16).OR.PFLAG(16))) THEN
          CALL WAMNORM(WMNFSG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(16),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM SWELL MEAN PERIOD: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(25).AND.(GFLAG(25).OR.FFLAG(25).OR.PFLAG(25))) THEN
          CALL WAMNORM(MWP1G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(25),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WAVE PERIOD (1): '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(26).AND.(GFLAG(26).OR.FFLAG(26).OR.PFLAG(26))) THEN
          CALL WAMNORM(MWP2G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(26),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WAVE PERIOD (2): '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(27).AND.(GFLAG(27).OR.FFLAG(27).OR.PFLAG(27))) THEN
          CALL WAMNORM(WSPRDG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(27),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WAVE DIRECTIONAL SPREAD: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(28).AND.(GFLAG(28).OR.FFLAG(28).OR.PFLAG(28))) THEN
          CALL WAMNORM(MWP1WG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(28),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WIND SEA PERIOD (1): '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(29).AND.(GFLAG(29).OR.FFLAG(29).OR.PFLAG(29))) THEN
          CALL WAMNORM(MWP1SG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(29),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN SWELL PERIOD (1): '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(30).AND.(GFLAG(30).OR.FFLAG(30).OR.PFLAG(30))) THEN
          CALL WAMNORM(MWP2WG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(30),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WIND SEA PERIOD (2): '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(31).AND.(GFLAG(31).OR.FFLAG(31).OR.PFLAG(31))) THEN
          CALL WAMNORM(MWP2SG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(31),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WIND SEA PERIOD (2): '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(32).AND.(GFLAG(32).OR.FFLAG(32).OR.PFLAG(32))) THEN
          CALL WAMNORM(WSPRDWG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(32),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN WIND SEA DIRECTIONAL SPREAD:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(33).AND.(GFLAG(33).OR.FFLAG(33).OR.PFLAG(33))) THEN
          CALL WAMNORM(WSPRDSG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(33),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN SWELL DIRECTIONAL SPREAD:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(34).AND.(GFLAG(34).OR.FFLAG(34).OR.PFLAG(34))) THEN
          CALL WAMNORM(C4G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(34),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM WAVE SPECTRAL KURTOSIS:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(35).AND.(GFLAG(35).OR.FFLAG(35).OR.PFLAG(35))) THEN
          CALL WAMNORM(BFG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(35),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM BENJAMIN-FEIR INSTABILITY:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(36).AND.(GFLAG(36).OR.FFLAG(36).OR.PFLAG(36))) THEN
          CALL WAMNORM(QPG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(36),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM WAVE SPECTRAL PEAKEDNESS:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(37).AND.(GFLAG(37).OR.FFLAG(37).OR.PFLAG(37))) THEN
          CALL WAMNORM(DEPTHG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(37),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MODEL BATHYMETRY:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(38).AND.(GFLAG(38).OR.FFLAG(38).OR.PFLAG(38))) THEN
          CALL WAMNORM(HMAXG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(38),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MAXIMUM WAVE HEIGHT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(39).AND.(GFLAG(39).OR.FFLAG(39).OR.PFLAG(39))) THEN
          CALL WAMNORM(TMAXG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(39),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MAXIMUM WAVE PERIOD:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(40).AND.(GFLAG(40).OR.FFLAG(40).OR.PFLAG(40))) THEN
          CALL WAMNORM(USTOKESG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(40),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM U-STOKESDRIFT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(41).AND.(GFLAG(41).OR.FFLAG(41).OR.PFLAG(41))) THEN
          CALL WAMNORM(VSTOKESG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(41),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM V-STOKESDRIFT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(42).AND.(GFLAG(42).OR.FFLAG(42).OR.PFLAG(42))) THEN
          CALL WAMNORM(UCURG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(42),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM U-CURRENT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        IF(NFLAG(43).AND.(GFLAG(43).OR.FFLAG(43).OR.PFLAG(43))) THEN
          CALL WAMNORM(VCURG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(43),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM V-CURRENT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=44
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(PHIEPSG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   NORMALIZED ENERGY FLUX INTO OCEAN: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,*) ' '
          ENDIF
        ENDIF

        ITG=45
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(PHIAWG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   NORMALIZED ENERGY FLUX INTO WAVES: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,*) ' '
          ENDIF
        ENDIF

        ITG=46
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(TAUOCG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   NORMALIZED STRESS INTO OCEAN: '
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,*) ' '
          ENDIF
        ENDIF


        ITG=46
        DO ITT = 1, NTRAIN
          ITG=ITG+1
          IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.
     &       FFLAG(ITG).OR.PFLAG(ITG))) THEN
            CALL WAMNORM(WHGTTRG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &       NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
            KTAG=KTAG+1
            IF(IRANK.EQ.1) THEN
              WRITE(IU06,*) '   WAMNORM TRAIN WAVE HEIGHTS: ',ITT
              WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
              WRITE(IU06,*) ' '
            ENDIF
          ENDIF

          ITG=ITG+1
          IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.
     &       FFLAG(ITG).OR.PFLAG(ITG))) THEN
            CALL WAMNORM(WDIRTRG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &       NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
            KTAG=KTAG+1
            IF(IRANK.EQ.1) THEN
              WRITE(IU06,*) '   WAMNORM TRAIN WAVE DIRECTION: ',ITT
              WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
              WRITE(IU06,*) ' '
            ENDIF
          ENDIF

          ITG=ITG+1
          IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.
     &       FFLAG(ITG).OR.PFLAG(ITG))) THEN
            CALL WAMNORM(MWPMTRG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &       NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
            KTAG=KTAG+1
            IF(IRANK.EQ.1) THEN
              WRITE(IU06,*) '   WAMNORM MEAN TRAIN PERIOD (-1): ',ITT
              WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
              WRITE(IU06,*) ' '
            ENDIF
          ENDIF

        ENDDO

        ITG=46+3*NTRAIN+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(STRNMSG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM MEAN SQUARE WAVE STRAIN IN ICE:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E10G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H10:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(RHOAG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM AIR DENSITY:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(WSTARG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM CONVECTIVE VELOCITY:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(CICG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM SEA ICE COVER:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(CIHG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM SEA ICE THICKNESS:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(C3G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM SPECTRAL SKEWNESS:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(NSSTG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM NEMO SST:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(NICCG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM NEMO SEA ICE COVER:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(NICTG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM NEMO SEA ICE THICKNESS:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(NUCUG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM NEMO U-COMPONENT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(NUCVG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM NEMO V-COMPONENT:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(WEFLXMG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM WAVE ENERGY FLUX MAGNITUDE:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(WEFLXDG,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM WAVE ENERGY FLUX DIRECTION:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E1012G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H1012:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E1214G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H1214:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E1417G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H1417:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E1721G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H1721:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E2125G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H2125:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

        ITG=ITG+1
        IF(NFLAG(ITG).AND.(GFLAG(ITG).OR.FFLAG(ITG).OR.PFLAG(ITG))) THEN
          CALL WAMNORM(E2530G,NGX,NGY,ZMISS,0.,FAVG,FMIN,FMAX,
     &     NAVG,LMESSPASS,IRANK,IPFGTBL(ITG),1,KTAG)
          KTAG=KTAG+1
          IF(IRANK.EQ.1) THEN
            WRITE(IU06,*) '   WAMNORM H2530:'
            WRITE(IU06,*) '  ',FAVG,FMIN,FMAX,NAVG
            WRITE(IU06,111) FAVG,FMIN,FMAX
          ENDIF
        ENDIF

      ENDIF

#ifdef ECMWF
      IF (LHOOK) CALL DR_HOOK('OUTWNORM',1,ZHOOK_HANDLE)
#endif

      RETURN
      END SUBROUTINE OUTWNORM
